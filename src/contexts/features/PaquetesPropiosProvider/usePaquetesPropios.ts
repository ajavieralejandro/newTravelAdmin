// src/contexts/features/PaquetesPropios/usePaquetesPropios.ts
import { usePaquetesPropiosContext } from './PaquetesPropiosProvider';

/**
 * Hook para consumir el contexto de Paquetes Propios.
 * Alias directo para mejorar legibilidad y mantener consistencia.
 */
export const usePaquetesPropios = usePaquetesPropiosContext;
